export enum ProjectStatus {
  deploying = "Deploying",
  deployed = "Deployed",
  failed = "Failed"
}
export enum ProjectSourceType {
  GITHUB = "GITHUB",
  GITLAB = "GITLAB",
  BITBUCKET = "BITBUCKET",
  MANUAL = "MANUAL"
}
export enum IntegrationPlatform {
  GITHUB = "GITHUB",
}